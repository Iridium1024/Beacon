# Agent Entry

Start with `BEACON.md`.

For ordinary onboarding, dispatch, status, and reverse-handoff tasks, read
`docs/agent/agent_entry.md`.

For provider CLI preflight or registered-session activation tasks, read
`docs/providers/provider_guides.md` and then the matching provider note.

Do not read or create internal migration records, historical smoke logs,
automation logs, or resource-pool paths while using this standalone Beacon
repository.
