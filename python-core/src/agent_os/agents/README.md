# Agents Module

Placeholder README for:
- `BaseAgent` shared composition
- role-based `PlannerAgent`, `ExecutorAgent`, and `ReviewerAgent`
- YAML-backed agent configuration loading
- registry-driven dynamic and hierarchical agent creation
