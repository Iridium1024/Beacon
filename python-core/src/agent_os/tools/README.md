# Tools Module

Placeholder README for:
- pluggable local tools
- sandbox path restriction
- validation before execution
- logging hooks around tool lifecycle
- built-in `FileTool`, `CodeExecutorTool`, and placeholder `GitTool`
