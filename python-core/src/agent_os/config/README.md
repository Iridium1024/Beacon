# Config Module

Placeholder README for Python core configuration composition.

Current boundary rule:
- `CoreSettings.deferred_features` is the canonical Python-core config surface for deferred collaboration paths.
- `finite_round_discussion`, `heartbeat`, `convergence`, `scheduler_heartbeat_path`, and `heartbeat_terminal_export_consumer` must remain disabled in the default runtime unless a future non-MVP composition root opts in explicitly.
